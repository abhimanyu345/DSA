/* Write your T-SQL query statement below */

select e.employee_id, e.department_id from (select employee_id, count(department_id) as count from employee group by employee_id) as z inner join employee as e  on e.employee_id = z.employee_id where z.count = 1

UNION

select employee_id, department_id from employee where primary_flag = 'Y'