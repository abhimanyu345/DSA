/* Write your T-SQL query statement below */

select e.name from(select managerId, count(managerId) as count from employee where id != managerId group by managerid having count(managerId)>=5) as z inner join employee as e on e.id = z.managerid