/* Write your T-SQL query statement below */

select name as employee from(select e.id as id, e.name as name , e.salary as salary, m.salary as managersalary  from employee as e inner join employee as m on e.managerid = m.id) as c where salary>managersalary